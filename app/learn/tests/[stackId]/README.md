# app/learn/tests/[stackId]

Dynamic test route for one stack.

- `page.tsx`: Client-side test flow with configurable question count, test mode, and answer scope
  (species/genus/family); handles scoring, learning-progress writes (including legacy fallback reads), and stack
  histogram updates.
