# app/(with-navbar)/groups

Learner-facing collection routes rendered inside the shared navbar shell.

- `[groupId]/page.tsx`: Collection detail page that shows the stacks inside one group as filterable, favorite-sorted
  cards with Learn and Take Test actions.
