# lib/pinkka

Pinkka API integration helpers and display utilities.

- `pinkka-api.ts`: Typed Pinkka API client and response models used during import, including taxonomy metadata needed
  for derived genus/family scientific labels.
- `pinkka-display.ts`: Presentation helpers for rendering Pinkka-linked entities in UI flows.
