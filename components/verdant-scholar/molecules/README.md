# Verdant Scholar molecules

Reusable stitched patterns built from the atoms: specimen cards, feature tiles, taxonomy filters, answer options, fact
panels, popup menus, section headings, and stack cards. Each molecule now also has its own Storybook entry so the
reusable patterns can be reviewed independently from the full-screen organism layouts.
